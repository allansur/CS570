
#include "pagetable.h"
using namespace std;


MAP * PageLookup(PAGETABLE *PAGETABLE, unsigned int LogicalAddress) {
    unsigned int = addr = LogicalToPage(LogicalAddress, bitMask[], 2);
    
}